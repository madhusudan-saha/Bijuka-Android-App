<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    >

    <item
        android:id="@+id/action_hindi"
        android:icon="@drawable/icon_back"
        android:title="@string/hindi"
        android:actionLayout="@layout/my_toolbar"

        app:showAsAction="never"/>
    <item
        android:id="@+id/action_english"
        android:orderInCategory="100"
        app:showAsAction="never"
        android:actionLayout="@layout/my_toolbar"
        android:title="@string/english" />
    >
    <item
        android:id="@+id/action_marathi"
        android:orderInCategory="100"
        app:showAsAction="never"
        android:actionLayout="@layout/my_toolbar"
        android:title="@string/marathi" />
    >
</menu>